#DATA SOURCE
spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/basi
    username: basi
    password: basi

#JPA
  jpa:
    hibernate.ddl-auto: update
    properties:
      hibernate.default_schema: test
      hibernate.integration.envers.enabled: false
